package arrays;

/**
 * ========================================================================
 * HINTS
 * ========================================================================
 *
 * SearchSortedAndRotatedArray
 * ---------------------------
 * Use (mid - 1) and (mid + 1) when subdividing search intervals
 *
 * SolveMaze
 * ---------
 * Declare empty path at top of method
 * If (start == exit), return path.add(start)
 * If start out-of-bounds, return empty path
 * If start is a wall, return empty path
 * Check remainingPath non-EMPTY (rather than non-null)
 *
 */
