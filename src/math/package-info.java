package math;

/**
 * ========================================================================
 *                                  HINTS
 * ========================================================================
 *
 * ModOperatorUsingSubtraction
 * ---------------------------
 * If (dividend < divisor), return dividend
 * while (dividend >= divisor && dividend >= subtrahend) {}
 *
 * RomanNumeralToDecimal
 * ---------------------
 * Work from right to left
 * If curr digit < prev digit, subtract, otherwise add
 */
